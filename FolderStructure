com.example.digibanker/
│
├── MainActivity.kt         // The main and only Activity. Its main role is to set up the UI content, theme, and navigation controller.
│
├── data/                   // Handles all data operations for the app.
│   │
│   ├── datasource/         // Manages the actual source of data.
│   │   └── local/
│   │       └── FakeDatabase.json  // Your JSON file would go in the `assets` folder.
│   │       └── JsonDataSource.kt  // Logic to read/parse the JSON file.
│   │
│   └── repository/         // Mediates between the data source and the rest of the app.
│       └── BankRepository.kt // Provides a clean API for the UI to access data (e.g., getBalance(), getTransactions()).
│
├── model/                  // Contains the data classes (the 'M' in MVVM).
│   ├── Account.kt          // e.g., data class Account(val number: String, val balance: Double)
│   └── Transaction.kt      // e.g., data class Transaction(...)
│
├── navigation/             // Defines the navigation routes and graph for the app.
│   ├── AppNavigation.kt    // Contains the NavHost and defines all possible screens.
│   └── Screen.kt           // Defines all navigation routes as a sealed class or object.
│
├── util/
│   └── Util.kt
│
└── ui/                     // Contains all UI-related code (the 'V' and 'VM' in MVVM).
    │
    ├── components/         // Shared, reusable Composable functions.
    │   ├── BalanceCard.kt
    │   └── ActionButton.kt
    │
    ├── screens/            // Each major feature/screen gets its own package.
    │   │
    │   ├── home/
    │   │   ├── HomeScreen.kt     // The Composable function for the home screen UI.
    │   │   └── HomeViewModel.kt  // The ViewModel for the home screen.
    │   │
    │   ├── transfer/
    │   │   ├── TransferScreen.kt
    │   │   └── TransferViewModel.kt
    │   │
    │   └── qris/
    │       ├── QrScannerScreen.kt
    │       └── QrScannerViewModel.kt
    │
    └── theme/                // Auto-generated by Android Studio for colors, typography, and shapes.
        ├── Color.kt
        ├── Theme.kt
        └── Type.kt
